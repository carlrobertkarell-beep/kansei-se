export function validateBlueprintLibrary(blueprints,exerciseIds){
 const known=new Set(exerciseIds||[]),errors=[];for(const [id,b] of Object.entries(blueprints||{})){for(const slot of [...(b.slots||[]),...(b.advancedSlots||[]),...(b.performanceSlots||[])])if(!known.has(slot))errors.push(id+': okänd övning '+slot);const g=b.progressionGraph;if(!g)continue;const ids=new Set((g.nodes||[]).map(n=>n.id));if(!g.start||!ids.has(g.start))errors.push(id+': ogiltig grafstart');for(const n of g.nodes||[])for(const ex of Object.keys(n.exercises||{}))if(!known.has(ex))errors.push(id+': graf refererar okänd övning '+ex);for(const e of g.edges||[])if(!ids.has(e.from)||!ids.has(e.to)||!['advance','regress'].includes(e.kind))errors.push(id+': ogiltig grafkant')}return {valid:errors.length===0,errors}}
