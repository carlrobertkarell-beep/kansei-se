export function renderEIQuality(data){const t=data?.totals||{},bp=data?.by_blueprint||[];const rate=(n,d)=>d?Math.round(n/d*1000)/10:0;return '<section class="ei-quality"><div class="quality-head"><div><p class="eyebrow">Exercise Intelligence · kvalitet</p><h2>Senaste '+Number(data?.days||30)+' dagarna</h2></div><span>'+Number(t.total||0).toLocaleString('sv-SE')+' motorbeslut</span></div><div class="quality-metrics">'+[['Advance',t.advance],['Hold',t.hold],['Regress',t.regress],['Eskalera',t.escalate],['Override',rate(data?.overridden,data?.reviewed)+' %'],['Ärende efter ändring',rate(data?.post_change_cases,t.applied)+' %']].map(x=>'<div><b>'+x[1]+'</b><span>'+x[0]+'</span></div>').join('')+'</div><details><summary>Per blueprint</summary><div class="quality-table">'+bp.map(x=>'<div><b>'+x.blueprint_id+'</b><span>'+x.total+' beslut</span><span>'+x.advance+' advance</span><span>'+x.hold+' hold</span><span>'+x.regress+' regress</span><span>'+x.escalate+' eskalera</span></div>').join('')+'</div></details><p class="quality-note">Måtten är kvalitetssignaler och driftdata. De är inte en klinisk ranking av blueprints eller behandlingsresultat.</p></section>'}
