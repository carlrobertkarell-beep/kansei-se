/* Reda 2.1 · clinician choices in, approved variants out. No diagnosis or progression inference. */
(function(root){'use strict';
const C=root.RedaCore,D=root.RedaData;
const blueprints={chair:{id:'chair',name:'Ben · vardagsfunktion',why:'Styrka, kontroll och balans för vardagens belastning.',slots:['chair','extension','calf','balance']},hip:{id:'hip',name:'Höft · grundplan',why:'Höftstyrka och kontroll med alternativ för golv eller stående.',slots:['bridge','abduction','chair']},shoulder:{id:'shoulder',name:'Axel · grundplan',why:'Kontrollerad belastning för axel och skuldror.',slots:['row','rotation']}};
const variants={chair:{supported:'support',standard:'free',high:'free'},extension:{supported:'easy',standard:'pause',high:'pause'},calf:{supported:'both',standard:'both',high:'single'},balance:{supported:'support',standard:'light',high:'light'},bridge:{supported:'low',standard:'pause',high:'pause'},abduction:{supported:'support',standard:'build',high:'build'},row:{supported:'light',standard:'light',high:'build'},rotation:{supported:'light',standard:'light',high:'build'}};
function scale(d,stage){d=C.clone(d);if(stage==='gentle'){d.sets=Math.min(d.sets,2);d.reps=Math.min(d.reps,10);if(d.hold)d.hold=Math.min(d.hold,20);}if(stage==='loaded'){d.sets=Math.min(4,d.sets+1);d.reps=Math.min(20,d.reps+2);if(d.hold)d.hold=Math.min(60,d.hold+10);}return d;}
function assemble(opts){const bp=blueprints[opts.blueprint]||blueprints.chair,capacity=['supported','standard','high'].includes(opts.capacity)?opts.capacity:'standard',stage=['gentle','build','loaded'].includes(opts.stage)?opts.stage:'build';let ids=bp.slots.slice();if(opts.floor===false)ids=ids.filter(x=>x!=='bridge');if(opts.band===false)ids=ids.filter(x=>!['row','rotation'].includes(x));if(!ids.length)throw Error('Förutsättningarna lämnar ingen godkänd övning i denna blueprint. Välj manuellt.');const p=C.template(opts.blueprint);p.title=opts.title||bp.name;p.goal=opts.goal||p.goal;p.reviewDate=opts.reviewDate||'';p.message='Programmet är anpassat efter det vi gick igenom vid besöket. Följ den här nivån fram till uppföljningen.';p.safety='Följ den ordinerade nivån och de individuella råd du fått. Ändra inte belastning eller svårighetsgrad på egen hand. Vid osäkerhet, kontakta kliniken.';p.items=ids.map(id=>{const e=D.exercises.find(x=>x.id===id),v=(variants[id]&&variants[id][capacity])||e.variants[0].id,i=C.makeItem(id,v,C.allowedSides(id,v)[0]);i.dose=scale(i.dose,stage);if(opts.experience==='familiar')i.note='Kort guidning: följ ordinerad dos och det utförande vi gick igenom.';return i;});return C.validatePlan(p);}
root.RedaPlanner={blueprints,assemble};
})(window);