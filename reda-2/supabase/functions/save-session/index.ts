import { withSupabase } from 'npm:@supabase/server@1.5.3'
import { corsHeaders } from 'jsr:@supabase/supabase-js@2.116.0/cors'
const json=(b:unknown,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{...corsHeaders,'Content-Type':'application/json'}})
const handler=withSupabase({auth:'user'},async(req,ctx)=>{if(req.method!=='POST')return json({error:'Method not allowed'},405);const userId=ctx.userClaims?.id;if(!userId)return json({error:'Sign in required'},401);const body=await req.json().catch(()=>null);if(!body?.client_session_id||!body?.plan_id||!['started','partial','completed','planned_rest'].includes(body?.status))return json({error:'Invalid request'},400);const {data,error}=await ctx.supabaseAdmin.rpc('reda_save_session_internal',{p_user_id:userId,p_client_session_id:body.client_session_id,p_plan_id:body.plan_id,p_status:body.status,p_started_at:body.started_at,p_completed_at:body.completed_at||null,p_payload:body.payload||{}});if(error)return json({error:'Session sync failed'},400);return json({ok:true,id:data})})
export default{fetch:(req:Request)=>req.method==='OPTIONS'?new Response('ok',{headers:corsHeaders}):handler(req)}